export type BlogPost = {
    id: string;
    title: string;
    excerpt: string;
    content?: string; // Optional for detailed blog content
    author?: string;  // Optional for author information
  };
  